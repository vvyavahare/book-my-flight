package com.pet.project.airline.booking.domain;

/**
 * Cabin class a passenger can choose. Each class has an admin-configurable seat fee and its
 * own baggage allowance / fee schedule.
 */
public enum SeatClass {
    ECONOMY,
    BUSINESS,
    FIRST
}
