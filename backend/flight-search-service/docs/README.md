# flight-search-service — diagrams

Living, code-coupled diagrams kept honest by guardrail tests. See
[docs/ARCHITECTURE.md](../../../docs/ARCHITECTURE.md) for how the mechanisms work.

### Logical architecture (packages)

Source: [`packages.puml`](packages.puml). Validated by `PackagesArchTest`: every package in
code must appear here, and ArchUnit asserts cross-package dependencies match the diagram.

[![Flight search package architecture](packages.svg)](packages.svg)

### Domain model (reflection-derived class diagram)

Generated by `DomainModelExtractorTest` from `domain/*.java`.

[![Flight search domain model](generated/DomainModel.svg)](generated/DomainModel.svg)

Click a diagram for the full-size SVG. To regenerate both SVGs after changing code or
`packages.puml`, run `./scripts/render-diagrams.sh` from the repository root.

For an interactive local preview in VS Code, install the **PlantUML** extension by jebbs,
open a `.puml` file, and press `Alt+D` (`Option+D` on macOS).
