package com.pet.project.airline.flightsearch;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

/**
 * Verifies the application context (JPA, seeded data, web layer) loads cleanly with
 * the default H2 profile.
 */
@SpringBootTest
class FlightSearchApplicationTests {

    @Test
    void contextLoads() {
    }
}
