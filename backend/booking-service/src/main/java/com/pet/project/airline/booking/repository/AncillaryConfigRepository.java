package com.pet.project.airline.booking.repository;

import com.pet.project.airline.booking.domain.AncillaryConfig;
import org.springframework.data.jpa.repository.JpaRepository;

public interface AncillaryConfigRepository extends JpaRepository<AncillaryConfig, String> {
}
