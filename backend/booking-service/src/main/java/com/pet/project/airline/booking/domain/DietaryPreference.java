package com.pet.project.airline.booking.domain;

/**
 * Dietary preference a meal option caters to, so travellers can filter by diet.
 */
public enum DietaryPreference {
    VEGETARIAN,
    VEGAN,
    NON_VEGETARIAN,
    GLUTEN_FREE
}
